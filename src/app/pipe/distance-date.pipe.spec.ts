import { DistanceDatePipe } from './distance-date.pipe';

describe('DistanceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
